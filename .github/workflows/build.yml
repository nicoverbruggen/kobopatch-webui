name: Build and test project

on:
  push:
    branches: [main, develop]
    tags: ['v*']

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install jq
        run: sudo apt-get install -y jq

      - name: Clone kobopatch source
        run: make setup-wasm

      - name: Run native tests
        run: |
          cd kobopatch-wasm/kobopatch-src
          go test ./...

      - name: Build WASM binary
        run: |
          make build-wasm
          test -f kobopatch-wasm/kobopatch.wasm || { echo "ERROR: kobopatch.wasm not found after build"; exit 1; }

      - name: Run WASM tests
        run: |
          cd kobopatch-wasm/kobopatch-src
          GOROOT="$(go env GOROOT)"
          if [ -f "$GOROOT/lib/wasm/go_js_wasm_exec" ]; then
            EXEC="$GOROOT/lib/wasm/go_js_wasm_exec"
          else
            EXEC="$GOROOT/misc/wasm/go_js_wasm_exec"
          fi
          GOOS=js GOARCH=wasm go test -exec="$EXEC" ./...

      - name: Check if integration test is needed
        id: check-e2e
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          elif git diff --name-only HEAD~1 HEAD | grep -qE '^(kobopatch-wasm/|web/|tests/|installables/|scripts/|patches/|\.github/workflows/)'; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Download test firmware
        if: steps.check-e2e.outputs.run == 'true' && env.GITEA_ACTIONS != 'true'
        run: |
          mkdir -p tests/cached_assets
          node -e "var c=require('./tests/firmware-config'); console.log(JSON.stringify([c.primary, c.secondary]))" | jq -c '.[]' | while IFS= read -r entry; do
            version=$(echo "$entry" | jq -r '.version')
            url=$(echo "$entry" | jq -r '.url')
            file="tests/cached_assets/kobo-update-${version}.zip"
            if [ ! -f "$file" ]; then
              echo "Downloading firmware $version..."
              curl -fL --progress-bar -o "$file" "$url"
            fi
          done

      - name: Build web app
        if: steps.check-e2e.outputs.run == 'true' && env.GITEA_ACTIONS != 'true'
        run: cd web && npm install && npm run build

      - name: Full integration test (WASM)
        if: steps.check-e2e.outputs.run == 'true' && env.GITEA_ACTIONS != 'true'
        run: make test-wasm

      - name: Set up installable assets
        if: steps.check-e2e.outputs.run == 'true' && env.GITEA_ACTIONS != 'true'
        run: make setup-installables

      - name: Validate download URLs
        if: steps.check-e2e.outputs.run == 'true' && env.GITEA_ACTIONS != 'true'
        run: |
          cd tests
          ./validate-dl-urls.sh

      - name: Full integration test (Playwright)
        if: steps.check-e2e.outputs.run == 'true' && env.GITEA_ACTIONS != 'true'
        run: make test-e2e
